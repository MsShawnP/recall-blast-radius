name: 'recall_blast_radius'
version: '1.0.0'
config-version: 2

profile: 'recall_blast_radius'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  recall_blast_radius:
    staging:
      +materialized: view
    genealogy:
      +materialized: table
